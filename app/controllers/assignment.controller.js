const db = require("../models");
const Assignment = db.assignments;
const Role = db.roles;
const Project = db.projects;
const Deliverable = db.deliverables;

// create method
exports.create = (assignment) => {
    return Assignment.create({
        raci: 
    })
}